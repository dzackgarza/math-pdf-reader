# M2 evidence: library UI over the bucket store

Recorded on 2026-09-23 on the development workstation (Arch Linux, WebKitGTK 2.52.6, Chromium 153 driven by Playwright).

## How the evidence is produced

```console
$ just library-screenshots
```

The recipe builds the web bundle, then `scripts/library_screenshots.py`:

1. makes three bucket roots in a temporary directory: an empty one; one holding `hand-copied.pdf`, a PDF with no embedded provenance; and one seeded with 1,000 PDFs by `scripts/seed_bucket.py`, which stores copies of the committed fixture PDFs in `tests/fixtures`, each under its own title, through `pdfbucket.store.store_pdf`, so each carries embedded provenance exactly as a capture does (nothing is downloaded);

2. serves each root with `src/server/serveBucket.ts` on a free port (never the configured 8770);

3. files the 300 most recent seeded items through the library API: six collections (two of them subcollections), four topics, seven tags, two notes, two saved searches;

4. lays the committed MinerU output (`tests/fixtures/mineru-ten-page-notes`) beside one item the way the extraction runner does, artifacts first and the Markdown last;

5. screenshots every state in the system Chromium, driven by Playwright, at 1600x1000, the mockups' size;

6. screenshots the library and the reader in the system WebKitGTK, the engine of the Tauri window, through `WebKitWebDriver` on a headless Weston display of 1400x900, the window's default size.

Playwright's own WebKit build is not used: its Linux build needs Ubuntu 24.04 libraries (ICU 74, `libxml2.so.2`, flite) that Arch does not ship.

## 1,000-item load and filter times

Seconds, from the run that produced the screenshots below (the seeded PDFs are copies of the four committed fixtures, 1 to 210 pages, 115 MB in all).

| Measurement | Seconds |
| --- | --- |
| Seed 1,000 PDFs through the store | 6.41 |
| `pdfbucket list` over 1,000 PDFs (one process) | 0.97 |
| `GET /api/library`, first request (index built from the files) | 1.12 |
| `GET /api/library`, second request (index cached, files re-stat'ed) | 0.017 |
| Window navigation to all 1,000 rows in the table | 0.56 |
| Typing a two-word filter until the table shows the matches | 0.14 |

The first request reads provenance out of every PDF; later requests re-read only PDFs whose modification time or size changed.
Filtering is the fzf filter over the item array in the browser; there is no search index.

## States

### Empty bucket

![Empty bucket](m2/library-empty.png)

### Loading

The library request is held open while this is taken.

![Loading](m2/library-loading.png)

### Store failure

A PDF under the root without embedded provenance fails the listing loudly.
The headline is the store's last line; the full store output is folded below it.

![Store failure](m2/library-error.png)

### Library, populated (mockup 1)

![Populated library with an item selected](m2/library-populated.png)

- **Sidebar:** Library and Unfiled (items in no collection); Collections (with a new-collection button), Topics, Tags, Saved Searches; Settings.

- **Top bar:** a search field (Ctrl+F), Filters, and Save search while a search is typed.

- **Table:** title with PDF mark, source domain, date added, tag pills with an overflow count.
  Double-click or Enter opens the reader; Delete deletes.

- **Inspector:** the title; one line with the source page link, date added and size; Details (collections, topics, tags, extraction) and Notes; Open and Send to Zotero.

- **Status bar:** PDFs, size on disk and unfiled count as icons with counts, and the capture indicator.

- **Row context menu:** Open, Open in Browser, Add to Collection (existing or new), Add Tag…, Retrieve Metadata, Send to Zotero, Copy Link (source page, PDF), Show in Folder (desktop window), Delete… (the PDF moves to the desktop trash).
  The webview's own page menu is suppressed outside text fields and selections.

- **Filing:** collections, topics and tags are added through one combobox that picks an existing entry or creates one from the typed name.

Filtering, the filters dialog, the notes tab, adding a tag from the inspector:

![Filtered library](m2/library-filtered.png) ![Filters dialog](m2/filters.png) ![Notes tab](m2/inspector-notes.png) ![A tag added from the inspector](m2/inspector-tag-added.png)

An item's extraction is derived from the files beside its PDF, never stored in the filing: `<key>.md` is the Markdown and marks a complete extraction, `<key>.extraction/` holds the further artifacts, and no Markdown means no extraction.
The inspector lists the Markdown and each artifact with its size:

![Extraction listed in the inspector](m2/inspector-extraction.png)

Ctrl+P goes to a PDF by fuzzy title (fzf); Ctrl+Shift+P runs a command:

![Go to PDF](m2/palette-items.png) ![Commands](m2/palette-commands.png)

In WebKitGTK at the window's 1400x900, the inspector overlays the table instead of narrowing it:

![Library in WebKitGTK](m2/webkit-library-populated.png)

`tests/library-e2e.test.ts` drives these workflows in Chromium against captured fixtures: creating a collection by typing its name in the inspector, filing and tagging from the row menu, deleting to the trash, Ctrl+F, Ctrl+P and Ctrl+Shift+P, and the reader's back and view address.

### Reader (mockup 2)

`/read/<key>` is PDF.js at full width under a bar with back, forward (Alt+← / Alt+→), the title and Copy link to this view.
The address carries PDF.js's open parameters (`#page=…&zoom=…`), updated on every view change, so reloading or sharing the address reopens the same view.
The page keeps M0's `citation_*` meta tags.

![Reader page](m2/reader.png) ![Reader page in WebKitGTK](m2/webkit-reader.png)

### Collections, topics, tags, saved searches (mockup 3)

![A collection](m2/organization-collections.png) ![A topic](m2/organization-topics.png) ![Tags](m2/organization-tags.png) ![A saved search](m2/organization-saved.png)

Creating a collection from the New collection dialog opens it:

![New collection dialog](m2/dialog-new-collection.png) ![The new collection](m2/organization-new-collection.png)

Compared with mockup 3: the Collections / Topics / Tags / Saved Searches tabs, subcollections nested under their parent, the selected entry's header (parent and name; New Subcollection, Rename, Delete for a collection; the stored query for a saved search) and its table.

### Settings (mockup 4)

The library folder (Show in Folder in the desktop window) and the version.

![Settings](m2/settings.png)

## Item titles

An item title comes from the best source available, in this order:

| Source (`titleSource`) | Where it comes from |
| --- | --- |
| `resolver` | the BibTeX from the identifier resolver whose pattern accepts the source URL, the PDF URL or an identifier embedded in the PDF (`/arXivID`, `/DOI`, `prism:doi`); citation-js turns its LaTeX into text |
| `pdf-metadata` | the PDF's own `/Title`, else its XMP `dc:title` |
| `capture-hint` | the title the capture offered: link text or page title |
| `filename` | the stored file name |

A new capture runs the resolvers before the server answers.
A resolver title is written into the stored PDF (`/Title` and `dc:title`, with `/PDFBucketTitleSource` and the XMP `title-source` beside it), so the index still reads everything from the files.
The other sources are read from the file on every listing.
A resolver that fails or times out (20 s per upstream request) leaves the title to the next source, and the capture stands.

`POST /api/items/<key>/metadata` ("Retrieve metadata") runs the resolvers again and answers `{outcome, item}`: `outcome` is `resolved` (plugin, identifier, title), `unidentified`, or `failed` (plugin, identifier, message); `item` is the library item as it now stands.
A failed resolver keeps the title the item had.
An unknown key answers 404.

`tests/server-titles.test.ts` runs the real resolver commands against a replay of their captured upstream responses (`tests/fixtures/resolvers`): an arXiv capture offered the link text `View PDF` takes the arXiv title; a DOI capture takes `The sphere packing problem in dimension 8` from BibTeX that writes `$8$`; with the resolvers unreachable the titles fall back to the PDF's own title and to the hint; Retrieve metadata resolves such an item later.

## Mockup elements not reproduced

Each has no home in the reused components or in PDF.js, or belongs to a later milestone; none is drawn as a control that does nothing.
The roadmap card's Decision Log records them.

| Mockup | Element | Why it is absent |
| --- | --- | --- |
| 1 | Authors column and inspector field | Stored items carry no creators until the resolver plugins (M3) supply them. |
| 1 | Status column (Cached, Offline, Needs re-fetch), Offline Cache view | Every stored item is a local file, so an Offline Cache view would equal Library. Re-fetch is `just rebuild-cache` (M5). |
| 1 | Quick filters (Unread, Cached Offline, Needs Re-fetch, Added This Week), sort menu, grid view | No read state or re-fetch state exists; sorting is by column header. |
| 1 | Import URL, Add Folder | Capture from the browser is the only ingest path in the roadmap. |
| 1 | Row checkboxes, bulk tag | The reused table selects one row. |
| 1 | First-page thumbnail, Related tab | Needs PDF rendering in the library window; PDF.js runs in the reader page. |
| 1 | Last verified, mirror URLs | Later enrichment (mirrors) and M5 (re-checking URLs). |
| 1, 4 | Chrome and Firefox toggles, browser capture panel | Each extension's toolbar popup holds its capture switch (M1); the status bar shows whether captures can land. |
| 2 | Provenance panel beside the reader | The reader is for reading; the source page and PDF links are in the inspector and the row menu, and the rest travels inside the PDF. |
| 4 | Provenance & Recovery, Zotero Integration, Server, Embedded Foundations sections | They describe the app instead of setting anything. |
| 2 | Collection list column beside the reader | The reader is its own page; it links back to the library. |
| 2 | Rebuild from metadata | The cache-rebuild recipe is M5. |
| 3 | Collection cards with descriptions, pins, offline toggle, recent activity | Collections carry a name and a parent only; entries render as the reused tree list. |
| 3 | Smart collection rule builder | Saved searches are stored queries of the filter dialog; the header shows the stored query. |
| 4 | Capture behaviour, file naming, cache size, retention, redownload, Zotero target and mapping controls | No setting exists behind them; the sections state what the app does. |
| 4 | Research jumping-off points panel | A planning checklist, not an app function. |
