# Inherit the global environment (~/.envrc) via direnv. This repo defines no
# secrets of its own; extraction provider keys (MISTRAL_API_KEY, MINERU_API_TOKEN)
# live in ~/.envrc. Run `direnv allow` after cloning.
source_up
