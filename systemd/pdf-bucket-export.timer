# Installed by `just provision`. Keeps the index export current, so a rebuild restores
# everything captured up to the last hour.
[Unit]
Description=Hourly PDF Bucket index export
Documentation=file://@REPO@/docs/m5.md

[Timer]
OnCalendar=hourly
Persistent=true

[Install]
WantedBy=timers.target
